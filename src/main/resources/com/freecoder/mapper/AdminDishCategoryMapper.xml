<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.freecoder.mapper.AdminDishCategoryMapper">
    <select id="getDishCategoryInfo" resultType="com.freecoder.model.DishCategory">
        select * from dish_category
        <where>
            restID = #{restID}
            order by dcID asc
        </where>
    </select>

    <insert id="addDishCategory">
        insert into dish_category (restID, dcName, dcOrder, dishNumber) values (#{restID},
        #{dcName},#{dcOrder},#{dishNumber})
    </insert>
</mapper>