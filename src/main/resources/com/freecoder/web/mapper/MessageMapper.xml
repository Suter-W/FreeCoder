<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.freecoder.web.mapper.MessageMapper">
    <insert id="addMessage">
        insert into message (msgContent, msgTime,restID) values (#{msgContent},#{msgTime},#{restID})
    </insert>
</mapper>